{
  "name": "AniWatch",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**"
    ]
  },
  "scripts": {
    "start": "lerna run start --stream",
    "apk:beta:release": "lerna run apk:beta:release --stream"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
